
### anti-single-step

* x86: single-stepping implemented with processor flag 
   * causes OS to run after every instruction
* can try to read flag directly
<hr class="vspace" />
* could also check timing
* could also try to replace OS's single-step handler

